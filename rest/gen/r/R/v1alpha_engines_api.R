#' Stability.ai REST API
#'
#' Welcome to the Stability.ai REST API developer preview!  Please note, this is an alpha version of our new REST API, and is subject to change according to your feedback.  Feedback during this test is crucial for us to understand what the pain points are and how we can improve it going forward.  Changes will be communicated here, as well as on our online REST API documentation, which can be found at: https://api.stability.ai/docs  Your DreamStudio API key will be required for authentication. You can learn more about API keys here: https://platform.stability.ai/docs/getting-started/authentication  If you encounter any issues, please let us know in the #API discord channel: https://discord.gg/stablediffusion 
#'
#' The version of the OpenAPI document: 0.1
#' Contact: info@stability.ai
#' Generated by: https://openapi-generator.tech
#'

#' @docType class
#' @title V1alphaEngines operations
#' @description openapi.V1alphaEngines
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ V1alphaEnginesListEngines } \emph{ list }
#' List all engines available to your organization/user
#'
#' \itemize{
#' \item \emph{ @param } organization character
#' \item \emph{ @returnType } \link{ListEnginesResponseBody} \cr
#'
#'
#' \item status code : 200 | OK response.
#'
#' \item return type : ListEnginesResponseBody 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  V1alphaEnginesListEngines  ####################
#'
#' library(openapi)
#' var.organization <- "org-123456" # character | Optional: Allows for requests to be scoped to an organization other than the user's default.  If not provided, the user's default organization will be used.
#'
#' #list
#' api.instance <- V1alphaEnginesApi$new()
#'
#' #Configure API key authorization: api_key_header_Authorization
#' api.instance$api_client$api_keys['Authorization'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$V1alphaEnginesListEngines(organization=var.organization)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
V1alphaEnginesApi <- R6::R6Class(
  "V1alphaEnginesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new V1alphaEnginesApi.
    #'
    #' @description
    #' Initialize a new V1alphaEnginesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' list
    #'
    #' @description
    #' list
    #'
    #' @param organization (optional)Optional: Allows for requests to be scoped to an organization other than the user's default.  If not provided, the user's default organization will be used. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ListEnginesResponseBody
    #' @export
    V1alphaEnginesListEngines = function(organization=NULL, data_file = NULL, ...) {
      api_response <- self$V1alphaEnginesListEnginesWithHttpInfo(organization, data_file = data_file, ...)
      resp <- api_response$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        api_response$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        api_response
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        api_response
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        api_response
      }
    },
    #' list
    #'
    #' @description
    #' list
    #'
    #' @param organization (optional) Optional: Allows for requests to be scoped to an organization other than the user's default.  If not provided, the user's default organization will be used.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ListEnginesResponseBody) with additional information such as HTTP status code, headers
    #' @export
    V1alphaEnginesListEnginesWithHttpInfo = function(organization=NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()

      body <- NULL
      url_path <- "/v1alpha/engines/list"
      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }

      resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
            write(httr::content(resp, "text", encoding = "UTF-8", simplifyVector = FALSE), data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(resp, "ListEnginesResponseBody", loadNamespace("openapi")),
          error = function(e) {
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserialized_resp_obj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
