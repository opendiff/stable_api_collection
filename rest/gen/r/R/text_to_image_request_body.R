#' Stability.ai REST API
#'
#' Welcome to the Stability.ai REST API developer preview!  Please note, this is an alpha version of our new REST API, and is subject to change according to your feedback.  Feedback during this test is crucial for us to understand what the pain points are and how we can improve it going forward.  Changes will be communicated here, as well as on our online REST API documentation, which can be found at: https://api.stability.ai/docs  Your DreamStudio API key will be required for authentication. You can learn more about API keys here: https://platform.stability.ai/docs/getting-started/authentication  If you encounter any issues, please let us know in the #API discord channel: https://discord.gg/stablediffusion 
#'
#' The version of the OpenAPI document: 0.1
#' Contact: info@stability.ai
#' Generated by: https://openapi-generator.tech
#'

#' @docType class
#' @title TextToImageRequestBody
#' @description TextToImageRequestBody Class
#' @format An \code{R6Class} generator object
#' @field cfg_scale  numeric [optional]
#' @field clip_guidance_preset  character [optional]
#' @field height  integer [optional]
#' @field sampler  character [optional]
#' @field samples  integer [optional]
#' @field seed  integer [optional]
#' @field steps  integer [optional]
#' @field text_prompts  list( \link{TextPrompt} )
#' @field width  integer [optional]
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TextToImageRequestBody <- R6::R6Class(
  "TextToImageRequestBody",
  public = list(
    `cfg_scale` = NULL,
    `clip_guidance_preset` = NULL,
    `height` = NULL,
    `sampler` = NULL,
    `samples` = NULL,
    `seed` = NULL,
    `steps` = NULL,
    `text_prompts` = NULL,
    `width` = NULL,
    #' Initialize a new TextToImageRequestBody class.
    #'
    #' @description
    #' Initialize a new TextToImageRequestBody class.
    #'
    #' @param text_prompts text_prompts
    #' @param cfg_scale How strictly the diffusion process adheres to the prompt text (higher values keep your image closer to your prompt). Default to 7.
    #' @param clip_guidance_preset clip_guidance_preset. Default to "NONE".
    #' @param height Height of the image (note: `height * width` must be <= 1 Megapixel). Default to 512.
    #' @param sampler Which sampler to use for the diffusion process. If this value is omitted we'll automatically select an appropriate sampler for you.
    #' @param samples Number of images to generate. Default to 1.
    #' @param seed Random noise seed (omit this option or use `0` for a random seed)
    #' @param steps Number of diffusion steps to run. Default to 50.
    #' @param width Width of the image (note: `height * width` must be <= 1 Megapixel). Default to 512.
    #' @param ... Other optional arguments.
    #' @export
    initialize = function(
        `text_prompts`, `cfg_scale` = 7, `clip_guidance_preset` = "NONE", `height` = 512, `sampler` = NULL, `samples` = 1, `seed` = NULL, `steps` = 50, `width` = 512, ...
    ) {
      if (!missing(`text_prompts`)) {
        stopifnot(is.vector(`text_prompts`), length(`text_prompts`) != 0)
        sapply(`text_prompts`, function(x) stopifnot(R6::is.R6(x)))
        self$`text_prompts` <- `text_prompts`
      }
      if (!is.null(`cfg_scale`)) {
        self$`cfg_scale` <- `cfg_scale`
      }
      if (!is.null(`clip_guidance_preset`)) {
        stopifnot(is.character(`clip_guidance_preset`), length(`clip_guidance_preset`) == 1)
        self$`clip_guidance_preset` <- `clip_guidance_preset`
      }
      if (!is.null(`height`)) {
        stopifnot(is.numeric(`height`), length(`height`) == 1)
        self$`height` <- `height`
      }
      if (!is.null(`sampler`)) {
        stopifnot(is.character(`sampler`), length(`sampler`) == 1)
        self$`sampler` <- `sampler`
      }
      if (!is.null(`samples`)) {
        stopifnot(is.numeric(`samples`), length(`samples`) == 1)
        self$`samples` <- `samples`
      }
      if (!is.null(`seed`)) {
        stopifnot(is.numeric(`seed`), length(`seed`) == 1)
        self$`seed` <- `seed`
      }
      if (!is.null(`steps`)) {
        stopifnot(is.numeric(`steps`), length(`steps`) == 1)
        self$`steps` <- `steps`
      }
      if (!is.null(`width`)) {
        stopifnot(is.numeric(`width`), length(`width`) == 1)
        self$`width` <- `width`
      }
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return TextToImageRequestBody in JSON format
    #' @export
    toJSON = function() {
      TextToImageRequestBodyObject <- list()
      if (!is.null(self$`cfg_scale`)) {
        TextToImageRequestBodyObject[["cfg_scale"]] <-
          self$`cfg_scale`
      }
      if (!is.null(self$`clip_guidance_preset`)) {
        TextToImageRequestBodyObject[["clip_guidance_preset"]] <-
          self$`clip_guidance_preset`
      }
      if (!is.null(self$`height`)) {
        TextToImageRequestBodyObject[["height"]] <-
          self$`height`
      }
      if (!is.null(self$`sampler`)) {
        TextToImageRequestBodyObject[["sampler"]] <-
          self$`sampler`
      }
      if (!is.null(self$`samples`)) {
        TextToImageRequestBodyObject[["samples"]] <-
          self$`samples`
      }
      if (!is.null(self$`seed`)) {
        TextToImageRequestBodyObject[["seed"]] <-
          self$`seed`
      }
      if (!is.null(self$`steps`)) {
        TextToImageRequestBodyObject[["steps"]] <-
          self$`steps`
      }
      if (!is.null(self$`text_prompts`)) {
        TextToImageRequestBodyObject[["text_prompts"]] <-
          lapply(self$`text_prompts`, function(x) x$toJSON())
      }
      if (!is.null(self$`width`)) {
        TextToImageRequestBodyObject[["width"]] <-
          self$`width`
      }

      TextToImageRequestBodyObject
    },
    #' Deserialize JSON string into an instance of TextToImageRequestBody
    #'
    #' @description
    #' Deserialize JSON string into an instance of TextToImageRequestBody
    #'
    #' @param input_json the JSON input
    #' @return the instance of TextToImageRequestBody
    #' @export
    fromJSON = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      if (!is.null(this_object$`cfg_scale`)) {
        self$`cfg_scale` <- this_object$`cfg_scale`
      }
      if (!is.null(this_object$`clip_guidance_preset`)) {
        self$`clip_guidance_preset` <- this_object$`clip_guidance_preset`
      }
      if (!is.null(this_object$`height`)) {
        self$`height` <- this_object$`height`
      }
      if (!is.null(this_object$`sampler`)) {
        self$`sampler` <- this_object$`sampler`
      }
      if (!is.null(this_object$`samples`)) {
        self$`samples` <- this_object$`samples`
      }
      if (!is.null(this_object$`seed`)) {
        self$`seed` <- this_object$`seed`
      }
      if (!is.null(this_object$`steps`)) {
        self$`steps` <- this_object$`steps`
      }
      if (!is.null(this_object$`text_prompts`)) {
        self$`text_prompts` <- ApiClient$new()$deserializeObj(this_object$`text_prompts`, "array[TextPrompt]", loadNamespace("openapi"))
      }
      if (!is.null(this_object$`width`)) {
        self$`width` <- this_object$`width`
      }
      self
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return TextToImageRequestBody in JSON format
    #' @export
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`cfg_scale`)) {
          sprintf(
          '"cfg_scale":
            %d
                    ',
          self$`cfg_scale`
          )
        },
        if (!is.null(self$`clip_guidance_preset`)) {
          sprintf(
          '"clip_guidance_preset":
            "%s"
                    ',
          self$`clip_guidance_preset`
          )
        },
        if (!is.null(self$`height`)) {
          sprintf(
          '"height":
            %d
                    ',
          self$`height`
          )
        },
        if (!is.null(self$`sampler`)) {
          sprintf(
          '"sampler":
            "%s"
                    ',
          self$`sampler`
          )
        },
        if (!is.null(self$`samples`)) {
          sprintf(
          '"samples":
            %d
                    ',
          self$`samples`
          )
        },
        if (!is.null(self$`seed`)) {
          sprintf(
          '"seed":
            %d
                    ',
          self$`seed`
          )
        },
        if (!is.null(self$`steps`)) {
          sprintf(
          '"steps":
            %d
                    ',
          self$`steps`
          )
        },
        if (!is.null(self$`text_prompts`)) {
          sprintf(
          '"text_prompts":
          [%s]
',
          paste(sapply(self$`text_prompts`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
          )
        },
        if (!is.null(self$`width`)) {
          sprintf(
          '"width":
            %d
                    ',
          self$`width`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      as.character(jsonlite::minify(paste("{", jsoncontent, "}", sep = "")))
    },
    #' Deserialize JSON string into an instance of TextToImageRequestBody
    #'
    #' @description
    #' Deserialize JSON string into an instance of TextToImageRequestBody
    #'
    #' @param input_json the JSON input
    #' @return the instance of TextToImageRequestBody
    #' @export
    fromJSONString = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      self$`cfg_scale` <- this_object$`cfg_scale`
      self$`clip_guidance_preset` <- this_object$`clip_guidance_preset`
      self$`height` <- this_object$`height`
      self$`sampler` <- this_object$`sampler`
      self$`samples` <- this_object$`samples`
      self$`seed` <- this_object$`seed`
      self$`steps` <- this_object$`steps`
      self$`text_prompts` <- ApiClient$new()$deserializeObj(this_object$`text_prompts`, "array[TextPrompt]", loadNamespace("openapi"))
      self$`width` <- this_object$`width`
      self
    },
    #' Validate JSON input with respect to TextToImageRequestBody
    #'
    #' @description
    #' Validate JSON input with respect to TextToImageRequestBody and throw an exception if invalid
    #'
    #' @param input the JSON input
    #' @export
    validateJSON = function(input) {
      input_json <- jsonlite::fromJSON(input)
      # check the required field `text_prompts`
      if (!is.null(input_json$`text_prompts`)) {
        stopifnot(is.vector(input_json$`text_prompts`), length(json_input$`text_prompts`) != 0)
        tmp <- sapply(input_json$`text_prompts`, function(x) stopifnot(R6::is.R6(x)))
      } else {
        stop(paste("The JSON input `", input, "` is invalid for TextToImageRequestBody: the required field `text_prompts` is missing."))
      }
    },
    #' To string (JSON format)
    #'
    #' @description
    #' To string (JSON format)
    #'
    #' @return String representation of TextToImageRequestBody
    #' @export
    toString = function() {
      self$toJSONString()
    }
  )
)

