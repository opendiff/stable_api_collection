#' Stability.ai REST API
#'
#' Welcome to the Stability.ai REST API developer preview!  Please note, this is an alpha version of our new REST API, and is subject to change according to your feedback.  Feedback during this test is crucial for us to understand what the pain points are and how we can improve it going forward.  Changes will be communicated here, as well as on our online REST API documentation, which can be found at: https://api.stability.ai/docs  Your DreamStudio API key will be required for authentication. You can learn more about API keys here: https://platform.stability.ai/docs/getting-started/authentication  If you encounter any issues, please let us know in the #API discord channel: https://discord.gg/stablediffusion 
#'
#' The version of the OpenAPI document: 0.1
#' Contact: info@stability.ai
#' Generated by: https://openapi-generator.tech
#'

#' @docType class
#' @title Engine
#' @description Engine Class
#' @format An \code{R6Class} generator object
#' @field description  character
#' @field id  character
#' @field name  character
#' @field type  character
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Engine <- R6::R6Class(
  "Engine",
  public = list(
    `description` = NULL,
    `id` = NULL,
    `name` = NULL,
    `type` = NULL,
    #' Initialize a new Engine class.
    #'
    #' @description
    #' Initialize a new Engine class.
    #'
    #' @param description description
    #' @param id Unique identifier for the engine
    #' @param name Name of the engine
    #' @param type The type of content this engine produces
    #' @param ... Other optional arguments.
    #' @export
    initialize = function(
        `description`, `id`, `name`, `type`, ...
    ) {
      if (!missing(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
        self$`description` <- `description`
      }
      if (!missing(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
        self$`id` <- `id`
      }
      if (!missing(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
        self$`name` <- `name`
      }
      if (!missing(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
        self$`type` <- `type`
      }
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return Engine in JSON format
    #' @export
    toJSON = function() {
      EngineObject <- list()
      if (!is.null(self$`description`)) {
        EngineObject[["description"]] <-
          self$`description`
      }
      if (!is.null(self$`id`)) {
        EngineObject[["id"]] <-
          self$`id`
      }
      if (!is.null(self$`name`)) {
        EngineObject[["name"]] <-
          self$`name`
      }
      if (!is.null(self$`type`)) {
        EngineObject[["type"]] <-
          self$`type`
      }

      EngineObject
    },
    #' Deserialize JSON string into an instance of Engine
    #'
    #' @description
    #' Deserialize JSON string into an instance of Engine
    #'
    #' @param input_json the JSON input
    #' @return the instance of Engine
    #' @export
    fromJSON = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      if (!is.null(this_object$`description`)) {
        self$`description` <- this_object$`description`
      }
      if (!is.null(this_object$`id`)) {
        self$`id` <- this_object$`id`
      }
      if (!is.null(this_object$`name`)) {
        self$`name` <- this_object$`name`
      }
      if (!is.null(this_object$`type`)) {
        self$`type` <- this_object$`type`
      }
      self
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return Engine in JSON format
    #' @export
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`description`)) {
          sprintf(
          '"description":
            "%s"
                    ',
          self$`description`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
          '"id":
            "%s"
                    ',
          self$`id`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
          '"name":
            "%s"
                    ',
          self$`name`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
          '"type":
            "%s"
                    ',
          self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      as.character(jsonlite::minify(paste("{", jsoncontent, "}", sep = "")))
    },
    #' Deserialize JSON string into an instance of Engine
    #'
    #' @description
    #' Deserialize JSON string into an instance of Engine
    #'
    #' @param input_json the JSON input
    #' @return the instance of Engine
    #' @export
    fromJSONString = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      self$`description` <- this_object$`description`
      self$`id` <- this_object$`id`
      self$`name` <- this_object$`name`
      self$`type` <- this_object$`type`
      self
    },
    #' Validate JSON input with respect to Engine
    #'
    #' @description
    #' Validate JSON input with respect to Engine and throw an exception if invalid
    #'
    #' @param input the JSON input
    #' @export
    validateJSON = function(input) {
      input_json <- jsonlite::fromJSON(input)
      # check the required field `description`
      if (!is.null(input_json$`description`)) {
        stopifnot(is.character(input_json$`description`), length(input_json$`description`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for Engine: the required field `description` is missing."))
      }
      # check the required field `id`
      if (!is.null(input_json$`id`)) {
        stopifnot(is.character(input_json$`id`), length(input_json$`id`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for Engine: the required field `id` is missing."))
      }
      # check the required field `name`
      if (!is.null(input_json$`name`)) {
        stopifnot(is.character(input_json$`name`), length(input_json$`name`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for Engine: the required field `name` is missing."))
      }
      # check the required field `type`
      if (!is.null(input_json$`type`)) {
        stopifnot(is.character(input_json$`type`), length(input_json$`type`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for Engine: the required field `type` is missing."))
      }
    },
    #' To string (JSON format)
    #'
    #' @description
    #' To string (JSON format)
    #'
    #' @return String representation of Engine
    #' @export
    toString = function() {
      self$toJSONString()
    }
  )
)

