#' Stability.ai REST API
#'
#' Welcome to the Stability.ai REST API developer preview!  Please note, this is an alpha version of our new REST API, and is subject to change according to your feedback.  Feedback during this test is crucial for us to understand what the pain points are and how we can improve it going forward.  Changes will be communicated here, as well as on our online REST API documentation, which can be found at: https://api.stability.ai/docs  Your DreamStudio API key will be required for authentication. You can learn more about API keys here: https://platform.stability.ai/docs/getting-started/authentication  If you encounter any issues, please let us know in the #API discord channel: https://discord.gg/stablediffusion 
#'
#' The version of the OpenAPI document: 0.1
#' Contact: info@stability.ai
#' Generated by: https://openapi-generator.tech
#'

#' @docType class
#' @title AccountResponseBody
#' @description AccountResponseBody Class
#' @format An \code{R6Class} generator object
#' @field email  character
#' @field id  character
#' @field organizations  list( \link{OrganizationMembership} )
#' @field profile_picture  character [optional]
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AccountResponseBody <- R6::R6Class(
  "AccountResponseBody",
  public = list(
    `email` = NULL,
    `id` = NULL,
    `organizations` = NULL,
    `profile_picture` = NULL,
    #' Initialize a new AccountResponseBody class.
    #'
    #' @description
    #' Initialize a new AccountResponseBody class.
    #'
    #' @param email The user's email
    #' @param id The user's ID
    #' @param organizations The user's organizations
    #' @param profile_picture The user's profile picture
    #' @param ... Other optional arguments.
    #' @export
    initialize = function(
        `email`, `id`, `organizations`, `profile_picture` = NULL, ...
    ) {
      if (!missing(`email`)) {
        stopifnot(is.character(`email`), length(`email`) == 1)
        self$`email` <- `email`
      }
      if (!missing(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
        self$`id` <- `id`
      }
      if (!missing(`organizations`)) {
        stopifnot(is.vector(`organizations`), length(`organizations`) != 0)
        sapply(`organizations`, function(x) stopifnot(R6::is.R6(x)))
        self$`organizations` <- `organizations`
      }
      if (!is.null(`profile_picture`)) {
        stopifnot(is.character(`profile_picture`), length(`profile_picture`) == 1)
        self$`profile_picture` <- `profile_picture`
      }
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return AccountResponseBody in JSON format
    #' @export
    toJSON = function() {
      AccountResponseBodyObject <- list()
      if (!is.null(self$`email`)) {
        AccountResponseBodyObject[["email"]] <-
          self$`email`
      }
      if (!is.null(self$`id`)) {
        AccountResponseBodyObject[["id"]] <-
          self$`id`
      }
      if (!is.null(self$`organizations`)) {
        AccountResponseBodyObject[["organizations"]] <-
          lapply(self$`organizations`, function(x) x$toJSON())
      }
      if (!is.null(self$`profile_picture`)) {
        AccountResponseBodyObject[["profile_picture"]] <-
          self$`profile_picture`
      }

      AccountResponseBodyObject
    },
    #' Deserialize JSON string into an instance of AccountResponseBody
    #'
    #' @description
    #' Deserialize JSON string into an instance of AccountResponseBody
    #'
    #' @param input_json the JSON input
    #' @return the instance of AccountResponseBody
    #' @export
    fromJSON = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      if (!is.null(this_object$`email`)) {
        self$`email` <- this_object$`email`
      }
      if (!is.null(this_object$`id`)) {
        self$`id` <- this_object$`id`
      }
      if (!is.null(this_object$`organizations`)) {
        self$`organizations` <- ApiClient$new()$deserializeObj(this_object$`organizations`, "array[OrganizationMembership]", loadNamespace("openapi"))
      }
      if (!is.null(this_object$`profile_picture`)) {
        self$`profile_picture` <- this_object$`profile_picture`
      }
      self
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return AccountResponseBody in JSON format
    #' @export
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`email`)) {
          sprintf(
          '"email":
            "%s"
                    ',
          self$`email`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
          '"id":
            "%s"
                    ',
          self$`id`
          )
        },
        if (!is.null(self$`organizations`)) {
          sprintf(
          '"organizations":
          [%s]
',
          paste(sapply(self$`organizations`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
          )
        },
        if (!is.null(self$`profile_picture`)) {
          sprintf(
          '"profile_picture":
            "%s"
                    ',
          self$`profile_picture`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      as.character(jsonlite::minify(paste("{", jsoncontent, "}", sep = "")))
    },
    #' Deserialize JSON string into an instance of AccountResponseBody
    #'
    #' @description
    #' Deserialize JSON string into an instance of AccountResponseBody
    #'
    #' @param input_json the JSON input
    #' @return the instance of AccountResponseBody
    #' @export
    fromJSONString = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      self$`email` <- this_object$`email`
      self$`id` <- this_object$`id`
      self$`organizations` <- ApiClient$new()$deserializeObj(this_object$`organizations`, "array[OrganizationMembership]", loadNamespace("openapi"))
      self$`profile_picture` <- this_object$`profile_picture`
      self
    },
    #' Validate JSON input with respect to AccountResponseBody
    #'
    #' @description
    #' Validate JSON input with respect to AccountResponseBody and throw an exception if invalid
    #'
    #' @param input the JSON input
    #' @export
    validateJSON = function(input) {
      input_json <- jsonlite::fromJSON(input)
      # check the required field `email`
      if (!is.null(input_json$`email`)) {
        stopifnot(is.character(input_json$`email`), length(input_json$`email`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for AccountResponseBody: the required field `email` is missing."))
      }
      # check the required field `id`
      if (!is.null(input_json$`id`)) {
        stopifnot(is.character(input_json$`id`), length(input_json$`id`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for AccountResponseBody: the required field `id` is missing."))
      }
      # check the required field `organizations`
      if (!is.null(input_json$`organizations`)) {
        stopifnot(is.vector(input_json$`organizations`), length(json_input$`organizations`) != 0)
        tmp <- sapply(input_json$`organizations`, function(x) stopifnot(R6::is.R6(x)))
      } else {
        stop(paste("The JSON input `", input, "` is invalid for AccountResponseBody: the required field `organizations` is missing."))
      }
    },
    #' To string (JSON format)
    #'
    #' @description
    #' To string (JSON format)
    #'
    #' @return String representation of AccountResponseBody
    #' @export
    toString = function() {
      self$toJSONString()
    }
  )
)

