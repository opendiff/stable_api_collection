#' Stability.ai REST API
#'
#' Welcome to the Stability.ai REST API developer preview!  Please note, this is an alpha version of our new REST API, and is subject to change according to your feedback.  Feedback during this test is crucial for us to understand what the pain points are and how we can improve it going forward.  Changes will be communicated here, as well as on our online REST API documentation, which can be found at: https://api.stability.ai/docs  Your DreamStudio API key will be required for authentication. You can learn more about API keys here: https://platform.stability.ai/docs/getting-started/authentication  If you encounter any issues, please let us know in the #API discord channel: https://discord.gg/stablediffusion 
#'
#' The version of the OpenAPI document: 0.1
#' Contact: info@stability.ai
#' Generated by: https://openapi-generator.tech
#'

#' @docType class
#' @title Error
#' @description Error Class
#' @format An \code{R6Class} generator object
#' @field fault  character
#' @field id  character
#' @field message  character
#' @field name  character
#' @field temporary  character
#' @field timeout  character
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Error <- R6::R6Class(
  "Error",
  public = list(
    `fault` = NULL,
    `id` = NULL,
    `message` = NULL,
    `name` = NULL,
    `temporary` = NULL,
    `timeout` = NULL,
    #' Initialize a new Error class.
    #'
    #' @description
    #' Initialize a new Error class.
    #'
    #' @param fault Is the error a server-side fault?
    #' @param id ID is a unique identifier for this particular occurrence of the problem.
    #' @param message Message is a human-readable explanation specific to this occurrence of the problem.
    #' @param name Name is the name of this class of errors.
    #' @param temporary Is the error temporary?
    #' @param timeout Is the error a timeout?
    #' @param ... Other optional arguments.
    #' @export
    initialize = function(
        `fault`, `id`, `message`, `name`, `temporary`, `timeout`, ...
    ) {
      if (!missing(`fault`)) {
        stopifnot(is.logical(`fault`), length(`fault`) == 1)
        self$`fault` <- `fault`
      }
      if (!missing(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
        self$`id` <- `id`
      }
      if (!missing(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
        self$`message` <- `message`
      }
      if (!missing(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
        self$`name` <- `name`
      }
      if (!missing(`temporary`)) {
        stopifnot(is.logical(`temporary`), length(`temporary`) == 1)
        self$`temporary` <- `temporary`
      }
      if (!missing(`timeout`)) {
        stopifnot(is.logical(`timeout`), length(`timeout`) == 1)
        self$`timeout` <- `timeout`
      }
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return Error in JSON format
    #' @export
    toJSON = function() {
      ErrorObject <- list()
      if (!is.null(self$`fault`)) {
        ErrorObject[["fault"]] <-
          self$`fault`
      }
      if (!is.null(self$`id`)) {
        ErrorObject[["id"]] <-
          self$`id`
      }
      if (!is.null(self$`message`)) {
        ErrorObject[["message"]] <-
          self$`message`
      }
      if (!is.null(self$`name`)) {
        ErrorObject[["name"]] <-
          self$`name`
      }
      if (!is.null(self$`temporary`)) {
        ErrorObject[["temporary"]] <-
          self$`temporary`
      }
      if (!is.null(self$`timeout`)) {
        ErrorObject[["timeout"]] <-
          self$`timeout`
      }

      ErrorObject
    },
    #' Deserialize JSON string into an instance of Error
    #'
    #' @description
    #' Deserialize JSON string into an instance of Error
    #'
    #' @param input_json the JSON input
    #' @return the instance of Error
    #' @export
    fromJSON = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      if (!is.null(this_object$`fault`)) {
        self$`fault` <- this_object$`fault`
      }
      if (!is.null(this_object$`id`)) {
        self$`id` <- this_object$`id`
      }
      if (!is.null(this_object$`message`)) {
        self$`message` <- this_object$`message`
      }
      if (!is.null(this_object$`name`)) {
        self$`name` <- this_object$`name`
      }
      if (!is.null(this_object$`temporary`)) {
        self$`temporary` <- this_object$`temporary`
      }
      if (!is.null(this_object$`timeout`)) {
        self$`timeout` <- this_object$`timeout`
      }
      self
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return Error in JSON format
    #' @export
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`fault`)) {
          sprintf(
          '"fault":
            %s
                    ',
          tolower(self$`fault`)
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
          '"id":
            "%s"
                    ',
          self$`id`
          )
        },
        if (!is.null(self$`message`)) {
          sprintf(
          '"message":
            "%s"
                    ',
          self$`message`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
          '"name":
            "%s"
                    ',
          self$`name`
          )
        },
        if (!is.null(self$`temporary`)) {
          sprintf(
          '"temporary":
            %s
                    ',
          tolower(self$`temporary`)
          )
        },
        if (!is.null(self$`timeout`)) {
          sprintf(
          '"timeout":
            %s
                    ',
          tolower(self$`timeout`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      as.character(jsonlite::minify(paste("{", jsoncontent, "}", sep = "")))
    },
    #' Deserialize JSON string into an instance of Error
    #'
    #' @description
    #' Deserialize JSON string into an instance of Error
    #'
    #' @param input_json the JSON input
    #' @return the instance of Error
    #' @export
    fromJSONString = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      self$`fault` <- this_object$`fault`
      self$`id` <- this_object$`id`
      self$`message` <- this_object$`message`
      self$`name` <- this_object$`name`
      self$`temporary` <- this_object$`temporary`
      self$`timeout` <- this_object$`timeout`
      self
    },
    #' Validate JSON input with respect to Error
    #'
    #' @description
    #' Validate JSON input with respect to Error and throw an exception if invalid
    #'
    #' @param input the JSON input
    #' @export
    validateJSON = function(input) {
      input_json <- jsonlite::fromJSON(input)
      # check the required field `fault`
      if (!is.null(input_json$`fault`)) {
        stopifnot(is.logical(input_json$`fault`), length(input_json$`fault`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for Error: the required field `fault` is missing."))
      }
      # check the required field `id`
      if (!is.null(input_json$`id`)) {
        stopifnot(is.character(input_json$`id`), length(input_json$`id`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for Error: the required field `id` is missing."))
      }
      # check the required field `message`
      if (!is.null(input_json$`message`)) {
        stopifnot(is.character(input_json$`message`), length(input_json$`message`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for Error: the required field `message` is missing."))
      }
      # check the required field `name`
      if (!is.null(input_json$`name`)) {
        stopifnot(is.character(input_json$`name`), length(input_json$`name`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for Error: the required field `name` is missing."))
      }
      # check the required field `temporary`
      if (!is.null(input_json$`temporary`)) {
        stopifnot(is.logical(input_json$`temporary`), length(input_json$`temporary`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for Error: the required field `temporary` is missing."))
      }
      # check the required field `timeout`
      if (!is.null(input_json$`timeout`)) {
        stopifnot(is.logical(input_json$`timeout`), length(input_json$`timeout`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for Error: the required field `timeout` is missing."))
      }
    },
    #' To string (JSON format)
    #'
    #' @description
    #' To string (JSON format)
    #'
    #' @return String representation of Error
    #' @export
    toString = function() {
      self$toJSONString()
    }
  )
)

